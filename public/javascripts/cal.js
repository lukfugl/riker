document.observe('dom:loaded', function() {
    // for each day
        // for each hour
            // new Ajax.InPlaceEditor(Label.buildHourUrl(day,hour), '', {onEditorMode = function(ipe, arg) { ipe.url = Label.buildFullUrl(day,hour,layer) }});
});
